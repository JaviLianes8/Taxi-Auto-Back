# ===========================================================
# 🪄 GitHub Actions Workflow (DISABLED / COMMENTED)
# ===========================================================
# This file shows how the Azure deployment pipeline works,
# but every line is commented so it won’t trigger or execute.
# ===========================================================

# name: Build and deploy Python app to Azure Web App - Taxi-auto-back

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read # Required for actions/checkout

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python version
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       # 🛠️ Local Build Section (Optional)
#       # Creates a virtual environment and installs dependencies.
#       - name: Create and Start virtual environment and Install dependencies
#         run: |
#           python -m venv antenv
#           source antenv/bin/activate
#           pip install -r requirements.txt
                
#       # Upload the project artifact for deployment jobs.
#       - name: Upload artifact for deployment jobs
#         uses: actions/upload-artifact@v4
#         with:
#           name: python-app
#           path: |
#             .
#             !antenv/

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     permissions:
#       id-token: write # Required for Azure login (OIDC)
#       contents: read # Required for checkout access

#     steps:
#       - name: Download artifact from build job
#         uses: actions/download-artifact@v4
#         with:
#           name: python-app
      
#       - name: Login to Azure
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_4F928F1FCA1E46B2AC2382E5BE55C711 }}
#           tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_8AC64171AD8E441994E7691C070FAA4D }}
#           subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_6024CF5C6A974097AF00AF9884409261 }}

#       - name: Deploy to Azure Web App
#         uses: azure/webapps-deploy@v3
#         id: deploy-to-webapp
#         with:
#           app-name: 'Taxi-auto-back'
#           slot-name: 'Production'
